import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    play : {

        flex : 1.5,
        width: '100%',
        flexDirection : 'row',
        justifyContent : 'space-evenly',
        alignItems : 'center',
    },

})